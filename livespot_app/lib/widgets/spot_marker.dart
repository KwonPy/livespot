import 'package:flutter/material.dart';

class SpotMarker extends StatelessWidget {
  const SpotMarker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.location_on, color: Colors.blue);
  }
}
