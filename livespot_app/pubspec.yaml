name: livespot_app
description: LiveSpot - AI 기반 실시간 여행 플랫폼
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_riverpod: ^2.6.1
  dio: ^5.7.0
  geolocator: ^13.0.2
  cached_network_image: ^3.4.1
  shimmer: ^3.0.0
  intl: ^0.19.0
  url_launcher: ^6.3.1
  shared_preferences: ^2.3.4
  permission_handler: ^11.3.1

  # Firebase - Firebase 프로젝트 설정 후 주석 해제
  # firebase_core: ^3.8.1
  # firebase_auth: ^5.3.4
  # cloud_firestore: ^5.6.5
  # firebase_storage: ^12.3.7
  # google_sign_in: ^6.2.2

  # 지도: MapTiler SDK JS (web/index.html에서 CDN 로드, Dart 패키지 불필요)

  # 추후 추가
  # image_picker: ^1.1.2
  # flutter_animate: ^4.5.0
  # lottie: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
